// an Enum for game Events
export enum GameEvent {
  CONNECT = 'connect',
  DISCONNECT = 'disconnect',
  STARTGAME = 'startGame',
}

export enum CardTypes {
  ATTACK = 'attack',
  DEFENCE = 'defence',
  MAGIC = 'magic',
}
